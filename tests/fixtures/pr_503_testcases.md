| Test Case ID | Function Name | Test Scenario | Input Values | Pre-Conditions | Test Steps | Expected Result | Post-Conditions | Test Type |
| ------------ | ------------- | ------------- | ------------ | -------------- | ---------- | --------------- | --------------- | --------- |
| TC01 | concatenate | Normal case - src fits within dest_size | char dest_arr[20]=""; char* src_ptr="world"; int dest_size_val=20 | dest_arr is empty, src_ptr is valid string | TEST_SCRIPT_INFO: Normal concatenation ; TEST CASE: concatenate ; Function call: concatenate(dest_arr, src_ptr, dest_size_val) ; Coverage: statement, branch | CHECK: return_value == 5 && strcmp(dest_arr, "world") == 0 | dest_arr contains "world", null-terminated | Normal |
| TC02 | concatenate | Truncation when src exceeds dest_size | char dest_arr[5]=""; char* src_ptr="hello world"; int dest_size_val=5 | dest_arr is empty, src_ptr is longer than dest_size | TEST_SCRIPT_INFO: Truncation on overflow ; TEST CASE: concatenate ; Function call: concatenate(dest_arr, src_ptr, dest_size_val) ; Coverage: statement, branch, boundary | CHECK: return_value == 4 && strcmp(dest_arr, "hell") == 0 | dest_arr contains "hell", null-terminated at index 4 | Boundary |
| TC03 | concatenate | Empty source string | char dest_arr[20]=""; char* src_ptr=""; int dest_size_val=20 | dest_arr is empty, src_ptr is empty string | TEST_SCRIPT_INFO: Empty src handling ; TEST CASE: concatenate ; Function call: concatenate(dest_arr, src_ptr, dest_size_val) ; Coverage: statement, branch, edge | CHECK: return_value == 0 && strcmp(dest_arr, "") == 0 | dest_arr is empty string, null-terminated at index 0 | Edge |
| TC04 | concatenate | dest_size equals 1 (only null terminator fits) | char dest_arr[1]=""; char* src_ptr="test"; int dest_size_val=1 | dest_arr has size 1, src_ptr is valid string | TEST_SCRIPT_INFO: Minimum dest_size ; TEST CASE: concatenate ; Function call: concatenate(dest_arr, src_ptr, dest_size_val) ; Coverage: statement, branch, boundary | CHECK: return_value == 0 && dest_arr[0] == '\0' | dest_arr contains only null terminator | Boundary |
| TC05 | concatenate | dest_size is zero | char dest_arr[10]=""; char* src_ptr="test"; int dest_size_val=0 | dest_size_val is 0 | TEST_SCRIPT_INFO: Zero dest_size ; TEST CASE: concatenate ; Function call: concatenate(dest_arr, src_ptr, dest_size_val) ; Coverage: statement, branch, boundary | CHECK: return_value == 0 && dest_arr[0] == '\0' | dest_arr null-terminated at index 0 | Boundary |
| TC06 | concatenate | Exact fit - src length equals dest_size minus 1 | char dest_arr[6]=""; char* src_ptr="hello"; int dest_size_val=6 | dest_arr is empty, src_ptr has length 5 | TEST_SCRIPT_INFO: Exact buffer fit ; TEST CASE: concatenate ; Function call: concatenate(dest_arr, src_ptr, dest_size_val) ; Coverage: statement, branch, boundary | CHECK: return_value == 5 && strcmp(dest_arr, "hello") == 0 | dest_arr contains "hello", null-terminated at index 5 | Boundary |
| TC07 | concatenate | Single character source | char dest_arr[10]=""; char* src_ptr="A"; int dest_size_val=10 | dest_arr is empty, src_ptr is single char | TEST_SCRIPT_INFO: Single char src ; TEST CASE: concatenate ; Function call: concatenate(dest_arr, src_ptr, dest_size_val) ; Coverage: statement, branch | CHECK: return_value == 1 && strcmp(dest_arr, "A") == 0 | dest_arr contains "A", null-terminated at index 1 | Normal |
| TC08 | concatenate | dest_size is 2, src longer than 1 char | char dest_arr[2]=""; char* src_ptr="ABCD"; int dest_size_val=2 | dest_arr has size 2, src_ptr has 4 chars | TEST_SCRIPT_INFO: Tight buffer with long src ; TEST CASE: concatenate ; Function call: concatenate(dest_arr, src_ptr, dest_size_val) ; Coverage: statement, branch, boundary | CHECK: return_value == 1 && strcmp(dest_arr, "A") == 0 | dest_arr contains "A", null-terminated at index 1 | Boundary |
| TC09 | concatenate | Large source string truncation | char dest_arr[10]=""; char* src_ptr="ABCDEFGHIJKLMNOP"; int dest_size_val=10 | dest_arr is empty, src_ptr has 16 chars | TEST_SCRIPT_INFO: Large src truncation ; TEST CASE: concatenate ; Function call: concatenate(dest_arr, src_ptr, dest_size_val) ; Coverage: statement, branch | CHECK: return_value == 9 && strcmp(dest_arr, "ABCDEFGHI") == 0 | dest_arr contains "ABCDEFGHI", null-terminated at index 9 | Normal |
| TC10 | concatenate | Source with special characters | char dest_arr[20]=""; char* src_ptr="Hi!\n\t"; int dest_size_val=20 | dest_arr is empty, src_ptr contains escape chars | TEST_SCRIPT_INFO: Special characters in src ; TEST CASE: concatenate ; Function call: concatenate(dest_arr, src_ptr, dest_size_val) ; Coverage: statement, branch, edge | CHECK: return_value == 5 && strcmp(dest_arr, "Hi!\n\t") == 0 | dest_arr contains "Hi!\n\t", null-terminated at index 5 | Edge |